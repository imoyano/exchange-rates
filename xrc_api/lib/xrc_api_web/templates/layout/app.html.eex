<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8"/>
	<meta http-equiv="X-UA-Compatible" content="IE=edge"/>
	<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
	<title>Currency exchange calculator</title>
	<link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
	<script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/jquery-3.7.1.min.js") %>"></script>
	<script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
</head>
<body>
<header>
</header>
<main role="main" class="container">
	<div class="exchange-form">
		<h2>Currency calculator</h2>
		<form id="exchange-form">

				<select id="origin_currency" name="origin_currency">
					<%= for {key, value} <- @options do %>
					<option value="<%= key %>" <%= if key == "CLP", do: "selected" %>><%= key %>: <%= value %></option>
					<% end %>
				</select>

				<input type="text" id="amount" name="amount" oninput="validateNumericInput(this)">

				<select id="goal_currency" name="goal_currency">
					<%= for {key, value} <- @options do %>
					<option value="<%= key %>" <%= if key == "USD", do: "selected" %>><%= key %>: <%= value %></option>
					<% end %>
				</select>

			<button type="button" id="calculate-button">Calculate</button>
		</form>
			<div id="result"></div>


	</div>
</main>
</body>
</html>
